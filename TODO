* Revisit the creation functions to ensure that we do not write
  anything with missing values of chr, strand, location.

* Consider renaming the getDB function to something like getConnection.

* makeBackground should be developed.

* Faster version of makeUIgene.

* Think about meta.id terminology.

* Add in functionality for ROC like curves like the PLoS paper.

* Make the interface to GenomeGraphs public.

* Perhaps add citations to the analyses pages (a CITATION file has been added).
